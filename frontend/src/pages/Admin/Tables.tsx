export default function AdminTables() {
  return (
    <section>
      <h2 style={{ marginTop: 0 }}>Quản lý bàn (base)</h2>
      <p>Endpoint khung: <code>/api/tables</code> và CRUD admin ở <code>/api/tables</code> (ADMIN)</p>
    </section>
  )
}

